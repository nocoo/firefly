# === Quality Gate: pre-push ===
# L1 Coverage + G1 Lint + G2 Security + Test DB check
# L2 API E2E runs in CI only (requires remote worker)
echo "▸ L1: unit tests (coverage ≥90%)"
bun run test:coverage
echo "▸ G1: lint"
bun run lint
echo "▸ G2: security"
bun run security
echo "▸ Test DB schema check (prevents CI E2E failures from missing migrations)"
bun scripts/check-test-db.ts
