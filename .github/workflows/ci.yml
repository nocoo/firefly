name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  # ─────────────────────────────────────────────────────
  # L1 + G1 + G2 — Core quality gate from base-ci
  # ─────────────────────────────────────────────────────
  quality:
    uses: nocoo/base-ci/.github/workflows/quality.yml@ab52d72877d386c9a8fd224e9ac868e88be90539
    with:
      runtime-version: "1.2.15"
      install-policy: "blocked"
      coverage-path: "coverage"
      extra-install-dirs: "worker"
      test-command: "bun run test:coverage"
      osv-config: "osv-scanner.toml"
    secrets: inherit

  # ─────────────────────────────────────────────────────
  # L2: API E2E (fully local — Miniflare D1 + filesystem R2)
  # Runs in PARALLEL with quality for faster CI.
  # ─────────────────────────────────────────────────────
  api-e2e:
    name: "L2 API E2E"
    uses: nocoo/base-ci/.github/workflows/test-job.yml@ab52d72877d386c9a8fd224e9ac868e88be90539
    with:
      runtime-version: "1.2.15"
      install-policy: "project"
      extra-install-dirs: "worker"
      timeout-minutes: 20
      command: "bun run test:e2e:api"
      env-json: '{"AUTH_URL": "http://localhost:17028"}'
    secrets:
      test-env-json: ${{ toJson(secrets) }}

  # ─────────────────────────────────────────────────────
  # L3: Browser E2E (fully local — same as L2)
  # Runs in PARALLEL with quality for faster CI.
  # ─────────────────────────────────────────────────────
  browser-e2e:
    name: "L3 Browser E2E"
    uses: nocoo/base-ci/.github/workflows/test-job.yml@ab52d72877d386c9a8fd224e9ac868e88be90539
    with:
      runtime-version: "1.2.15"
      install-policy: "project"
      extra-install-dirs: "worker"
      browser: "chromium"
      timeout-minutes: 25
      command: "bun run test:e2e:browser"
      env-json: '{"AUTH_URL": "http://localhost:27028"}'
    secrets:
      test-env-json: ${{ toJson(secrets) }}

  # ─────────────────────────────────────────────────────
  # Worker: Edge Worker unit tests
  # Runs in PARALLEL with quality for faster CI.
  # ─────────────────────────────────────────────────────
  worker-tests:
    name: "Worker Tests"
    uses: nocoo/base-ci/.github/workflows/test-job.yml@ab52d72877d386c9a8fd224e9ac868e88be90539
    with:
      runtime-version: "1.2.15"
      install-policy: "project"
      working-directory: "worker"
      timeout-minutes: 10
      command: "bun run test"
